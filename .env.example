# Copy to .env and adjust. `docker compose up -d` gives you a matching Postgres.
DATABASE_URL="postgresql://pulse:pulse@localhost:5432/pulse?schema=public"

# 32+ char random string. Generate: `openssl rand -base64 32`
AUTH_SECRET="change-me"
